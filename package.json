{
  "name": "typed-json-schema",
  "version": "4.0.4",
  "description": "",
  "main": "index",
  "typings": "index.d.ts",
  "scripts": {
    "mocha": "ts-mocha test/**/*.spec.ts",
    "test-typedefs": "typings-checker --project test-typedefs/tsconfig.json --allow-expect-error test-typedefs/*.ts",
    "test": "npm run mocha && npm run test-typedefs",
    "clean": "rm *.js *.d.ts; rm -rf ext/;true",
    "tsc": "tsc --declaration",
    "build": "npm run clean && npm run tsc",
    "prepublishOnly": "npm test && npm run build",
    "postpublishOnly": "npm run clean"
  },
  "bin": {
    "tjs": "generate.js"
  },
  "files": [
    "*.js",
    "*.d.ts",
    "ext"
  ],
  "author": "Sam Beran (sberan@gmail.com)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git@github.com:sberan/typed-json-schema.git"
  },
  "dependencies": {
    "ajv": "5.2.2"
  },
  "devDependencies": {
    "@types/ajv": "^1.0.0",
    "@types/mocha": "8.0.3",
    "chai": "4.2.0",
    "mocha": "8.2.0",
    "ts-mocha": "8.0.0",
    "typescript": "4.0.5",
    "typings-checker": "2.0.0"
  }
}
